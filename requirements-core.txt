# Ядро фабрики кода — зависимости Python (core)
# Фабрика (scripts/code-factory, orchestration, memory, kanban, router, guard)
# использует ТОЛЬКО стандартную библиотеку Python 3.11. Внешних пакетов не требуется.
# Этот файл пуст намеренно: requirements.txt существует для перефиксации минимального
# набора venv. При установке pip install -r requirements-core.txt ничего не ставится.
# Если какой-то модуль ядра начнёт требовать внешний пакет — внести его сюда С ОБОСНОВАНИЕМ.